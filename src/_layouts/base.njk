<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} | {{ site.title }}{% else %}{{ site.title }}{% endif %}</title>
    <meta name="color-scheme" content="light dark">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=PT+Serif&family=Rubik:ital,wght@0,300;0,500;0,900;1,300&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ '/assets/css/main.css' | url }}">
    {% include 'meta.njk' %}
    <script>{% include 'initialtheme.njk' %}</script>
</head>
<body>
    <a href="#main" class="sr-skip-link">skip to main content</a>

    {% include 'navbar.njk' %}

    <main id="main" class="main" role="main">
        {{content | safe }}
    </main>

    {% include 'footer.njk' %}

    <script src="{{ '/assets/js/main.js' | url }}" defer></script>
</body>
</html>